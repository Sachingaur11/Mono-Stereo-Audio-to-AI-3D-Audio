services:
  - type: web
    name: my-python-service
    env: python
    region: oregon
    buildCommand: 
      - pip install -r requirements.txt
    startCommand: python app.py
    pythonVersion: 3.10.12  # Specify Python 3.10 version
